# Base URL for API calls. Defaults to /api, which the Vite dev server proxies
# to http://localhost:4000 (see vite.config.ts) so the browser never makes a
# cross-origin request. Only override this if the API is actually served
# same-origin some other way (e.g. a production reverse proxy).
VITE_API_URL=/api
